{
  "name": "talo-dictionary",
  "version": "0.1.0",
  "private": true,
  "description": "Dictionary generator for Talo — joins data/lexicon.tsv + data/concepts.tsv into a structured bilingual dictionary (JSON for the web tool; Markdown for the book/site) and acts as a cross-file data integrity gate.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "node --experimental-strip-types src/build.ts",
    "check": "node --experimental-strip-types src/build.ts --check",
    "test": "node --experimental-strip-types --test test/dictionary.test.ts"
  },
  "engines": {
    "node": ">=22.6.0"
  }
}
