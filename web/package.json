{
  "name": "talo-web",
  "version": "0.1.0",
  "private": true,
  "description": "The Talo public site: a static landing page, the rendered 'Teach Yourself' book, and a client-side word-lookup tool over the generated dictionary. Builds to web/dist/ for GitHub Pages.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "node build/build-site.mjs",
    "check": "node build/build-site.mjs --check"
  },
  "engines": {
    "node": ">=22.6.0"
  }
}
